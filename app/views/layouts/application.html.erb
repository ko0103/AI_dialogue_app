<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "AI_dialogue" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <meta name="turbo-prefetch" content="false">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <%= favicon_link_tag("favicon.ico") %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= display_meta_tags(default_meta_tags) %>

    <script src="https://kit.fontawesome.com/7fc2da0e12.js" crossorigin="anonymous"></script>
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-DLX8NMJLYQ"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-DLX8NMJLYQ');
    </script>
  </head>

  <body>
    <% if controller_name == "top_pages" && action_name == "top" %>
      <div class="bg-[url('AI_dialogue.png')] bg-cover bg-fixed bg-center min-h-screen">
    <% else %>
      <div class="bg-[url('back_ground.png')] bg-cover bg-fixed bg-center h-full">
    <% end %>
      <div class="flex flex-col min-h-screen">
        <header class="flex items-center justify-between h-14 bg-sky-950">
          <%= render "shared/header" %>
        </header>
        <% if flash[:notice] %>
          <p class="notice bg-green-300 text-center"><%= notice %></p>
        <% end %>
        <% if flash[:alert] %>
          <p class="alert bg-red-500 text-white text-center"><%= alert %></p>
        <% end %>
        <main class="flex-grow">
          <%= yield %>
        </main>
        <footer>
          <%= render "shared/footer" %>
        </footer>
      </div>
    </div>
  </body>
</html>
